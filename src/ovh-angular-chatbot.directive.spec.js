"use strict";
describe("directive: chatbot", () => {
    beforeEach(angular.mock.module("chatbot"));

    describe("Test", () => {
        it("should prove that test running...", () => {
            expect(true).not.toBe(false);
        });
    });
});
